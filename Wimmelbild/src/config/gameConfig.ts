import type { GameConfig } from '../types/interfaces';

export const gameConfig: GameConfig = {
    POKEMON_COUNT: 25,
    POKEMON_SIZE: 50,
    SCREEN_PADDING: 50,
    OVERLAP_THRESHOLD: 40,
    TARGET_PANEL_WIDTH: 150,
    TARGET_PANEL_HEIGHT: 150,
    MAX_POKEMON_ID: 151
}; 